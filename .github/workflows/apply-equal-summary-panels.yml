name: Apply equal summary panels

on:
  push:
    branches:
      - ui/responsive-main-layout
    paths:
      - "scripts/patch_equal_summary_panels.py"

permissions:
  contents: write

jobs:
  patch-layout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ui/responsive-main-layout
      - run: python scripts/patch_equal_summary_panels.py
      - run: python -m py_compile PingerApp/PingerApp.py
      - run: git diff --check
      - shell: bash
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add PingerApp/PingerApp.py
          git commit -m "Equalize summary panel widths"
          git push origin HEAD:ui/responsive-main-layout
