name: Apply raw output UI cleanup

on:
  push:
    branches:
      - ui/responsive-main-layout
    paths:
      - "scripts/patch_raw_output_ui.py"

permissions:
  contents: write

jobs:
  patch-ui:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ui/responsive-main-layout
      - run: python scripts/patch_raw_output_ui.py
      - run: python -m py_compile PingerApp/PingerApp.py
      - run: git diff --check
      - shell: bash
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add PingerApp/PingerApp.py
          git commit -m "Make raw diagnostic output secondary"
          git push origin HEAD:ui/responsive-main-layout
