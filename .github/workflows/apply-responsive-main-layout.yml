name: Apply responsive main layout

on:
  push:
    branches:
      - ui/responsive-main-layout
    paths:
      - "scripts/patch_responsive_main_layout.py"

permissions:
  contents: write

jobs:
  patch-layout:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v7
        with:
          ref: ui/responsive-main-layout

      - name: Apply responsive layout patch
        run: python scripts/patch_responsive_main_layout.py

      - name: Validate Python
        run: |
          python -m py_compile PingerApp/PingerApp.py
          git diff --check

      - name: Remove temporary patch files and commit
        shell: bash
        run: |
          rm -f scripts/patch_responsive_main_layout.py .github/workflows/apply-responsive-main-layout.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "Improve responsive startup and summary layout"
          git push origin HEAD:ui/responsive-main-layout
